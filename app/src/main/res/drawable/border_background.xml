<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <stroke
        android:width="2dp"
        android:color="@color/red" /> <!-- Màu viền -->
    <corners android:radius="20dp" /> <!-- Bo viền -->
</shape>